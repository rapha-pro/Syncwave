# Backend API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# OAuth Client IDs
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# OAuth Redirect URIs
NEXT_PUBLIC_SPOTIFY_REDIRECT_URI=http://localhost:3000/auth/spotify/callback
NEXT_PUBLIC_YOUTUBE_REDIRECT_URI=http://localhost:3000/auth/youtube/callback

# Production Notes:
# - Update NEXT_PUBLIC_API_BASE_URL to your backend API URL (e.g., https://syncwave-backend.onrender.com)
# - Update redirect URIs to match your production domain (e.g., https://your-app.vercel.app/auth/spotify/callback)
# - Ensure these redirect URIs are also added to Spotify Developer Dashboard and Google Cloud Console
# - Use the SAME client IDs as in your backend .env file

